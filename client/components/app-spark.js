import sparkScrollFactory from 'react-spark-scroll/lib/spark-scroll-gsap';


export default sparkScrollFactory({
  invalidateAutomatically: true

  // we could add additional options in here...

  // setup: {...}
  // formulas: {...}
  // actionProps: {...}
});
